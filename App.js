import React from 'react';
import {
  SafeAreaView,
  ScrollView,
  StatusBar,
  StyleSheet,
  Text,
  useColorScheme,
  View,
} from 'react-native';
import Form from './src/form'

const App = () => {
  return (
    <SafeAreaView >
      <Form />
    </SafeAreaView>
  );
};


export default App;
